module(
    query(and(
        unify(g, 1410),
        unify(w, 997),
        unify(gw_formula, div(g, w)),
        eval(gw_formula, gw),
        print('srt(2) w przybliżeniu wynosi {}', gw)
    ))
)

