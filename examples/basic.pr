module(
    author('Eryk Andrzejewski'),
    query(and(
        print('Welcome to the basic example'),
        unify(x, 3),
        unify(y, 7),
        eval(add(x, y), z)
        print(add(x, y), z)
    ))
)

